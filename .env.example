# PostgreSQL
POSTGRES_USER=cyworld
POSTGRES_PASSWORD=cyworld1234
POSTGRES_DB=cyworld

# Server
JWT_SECRET=maxt-cyworld-secret-change-in-production
PORT=3001

# Supabase Storage (image uploads) — set real values in Render, never commit them
# SUPABASE_URL=
# SUPABASE_SERVICE_ROLE_KEY=

# Comma-separated production origins allowed by CORS (e.g. Vercel domain)
# CORS_ORIGIN=https://maxt-cyworld-client.vercel.app

# Database URL (composed from above)
DATABASE_URL=postgresql://cyworld:cyworld1234@db:5432/cyworld
