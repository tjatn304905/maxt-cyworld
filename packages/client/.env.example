# API server origin for production builds (e.g. Render URL, no trailing slash).
# Leave unset in dev — the Vite proxy forwards /api to the local server.
# VITE_API_URL=https://maxt-cyworld.onrender.com
